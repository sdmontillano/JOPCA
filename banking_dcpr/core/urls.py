from django.urls import path
from . import views

urlpatterns = [
    # Dashboard and templates (if any)
    # path("dashboard/", views.dashboard, name="dashboard"),
    # path("transactions/add/", views.add_transaction, name="add_transaction"),
]
