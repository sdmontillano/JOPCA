{
  "version": 2,
  "builds": [
    {
      "src": "frontend_admin/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/frontend_admin/$1"
    }
  ]
}
