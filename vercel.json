{
  "version": 2,
  "builds": [
    {
      "src": "frontend_admin/package.json",
      "use": "@vercel/next",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "buildCommand": "cd frontend_admin && npm run build",
  "outputDirectory": "frontend_admin/dist"
}
